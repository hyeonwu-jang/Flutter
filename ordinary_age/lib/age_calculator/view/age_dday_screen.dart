import 'package:flutter/material.dart';

class AgeDdayScreen extends StatelessWidget {
  const AgeDdayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ㅁㄴㅇㄹ'),
    );
  }
}
